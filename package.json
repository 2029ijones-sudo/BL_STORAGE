{
  "name": "bl-storage-api",
  "version": "2.0.0",
  "description": "Better Local Storage - Advanced Cloudflare Worker Implementation",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-vm-modules test.js",
    "build": "esbuild worker.js --bundle --outfile=dist/worker.js --format=esm --target=es2022"
  },
  "dependencies": {
    "esbuild": "^0.19.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  },
  "keywords": [
    "bl-storage",
    "quantum-database",
    "cloudflare-workers",
    "advanced-storage"
  ],
  "author": "BL Storage Team",
  "license": "BL-PROPRIETARY"
}
